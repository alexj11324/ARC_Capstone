# Archive Folder

This folder stores historical notes and large reference artifacts that are
useful for record-keeping but should not stay at repository root.

Keep active project documentation under `docs/` and keep root focused on
entry-point files (`README.md`, run configs, and core contracts).
